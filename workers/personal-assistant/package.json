{
  "name": "personal-assistant-worker",
  "version": "1.0.0",
  "description": "Personal AI Assistant - Cloudflare Worker",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --format=esm --outfile=dist/index.js --external:__STATIC_CONTENT --platform=neutral",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test dist/*.test.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.27.0",
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250217.0",
    "esbuild": "^0.21.0",
    "typescript": "^5.3.0",
    "wrangler": "^3.40.0"
  }
}
